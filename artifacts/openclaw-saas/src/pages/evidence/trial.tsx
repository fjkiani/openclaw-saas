import { useEffect,useState } from "react";import{Link,useParams}from"wouter";import{Layout,PageHeader}from"@/components/Layout";import{BoundaryBanner,EvidenceCard,EvidenceStatusChip,EmptyEvidence}from"@/components/evidence/EvidencePrimitives";import{evidenceApi}from"@/lib/evidenceApi";
export default function EvidenceTrialPage(){const{nctId}=useParams<{nctId:string}>();const[data,setData]=useState<any>();const[error,setError]=useState("");useEffect(()=>{evidenceApi.trial(nctId).then(setData).catch(x=>setError(String(x)))},[nctId]);return <Layout><PageHeader title={nctId.toUpperCase()} subtitle="ClinicalTrials.gov facts and AACR linkage evidence"/><div className="max-w-6xl mx-auto p-6 space-y-5"><BoundaryBanner/>{error&&<div data-testid="trial-error" className="text-red-300">{error}</div>}{!data&&!error&&<EmptyEvidence text="Loading trial evidence…"/>}{data&&<><div className="flex items-center gap-2"><EvidenceStatusChip status="VERIFIED_REGISTRY_FACT"/><span className="text-xs text-muted-foreground">Registry record exists. This alone does not confirm target or abstract linkage.</span></div><section><h2 className="font-mono font-bold mb-3">Registry facts</h2><div className="grid lg:grid-cols-2 gap-3">{data.registry_facts.map((c:any)=><EvidenceCard key={c.receipt_id} claim={c}/>)}</div></section><section><h2 className="font-mono font-bold mb-3">AACR abstract linkages</h2>{data.abstract_linkages.length?<div className="space-y-3">{data.abstract_linkages.map((l:any)=><article key={l.receipt_id} className="border border-border rounded bg-card p-4"><div className="flex justify-between"><Link href={`/evidence/abstracts/${encodeURIComponent(l.source_record_id)}`} className="text-primary font-mono hover:underline">{l.source_record_id}</Link><EvidenceStatusChip status={l.status_chip}/></div><p className="font-mono text-sm mt-2">{l.linkage_state}</p><Link href={`/evidence/traces/${l.receipt_id}`} className="text-xs text-primary hover:underline">Inspect deterministic linkage receipt</Link></article>)}</div>:<div data-testid="unlinked-trial" className="border border-amber-500/30 bg-amber-500/5 rounded p-4"><EvidenceStatusChip status="LINKAGE_UNVERIFIED"/><p className="text-sm mt-2">No AACR abstract linkage has been established for this registry study.</p></div>}</section></>}</div></Layout>}
